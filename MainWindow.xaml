<Window x:Class="DesktopSplit.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DesktopSplit 设置"
        Width="900"
        Height="720"
        MinWidth="760"
        MinHeight="620"
        Background="{DynamicResource WindowBackgroundBrush}"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen"
        LocationChanged="OnWindowLocationChanged"
        Closing="OnWindowClosing">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,22">
            <TextBlock Text="DesktopSplit" FontSize="26" FontWeight="SemiBold" Foreground="#172033" />
            <TextBlock Text="把一块屏幕划成多个可独立最大化的区域" Margin="0,5,0,0" Foreground="{DynamicResource MutedTextBrush}" FontSize="13" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.45*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="20" Margin="0,0,18,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,14">
                        <TextBlock Text="预览" FontSize="16" FontWeight="SemiBold" Foreground="#172033" />
                        <TextBlock Text="  拖动分隔线调整比例" Foreground="{DynamicResource MutedTextBrush}" VerticalAlignment="Bottom" Margin="8,0,0,1" FontSize="12" />
                    </StackPanel>
                    <Border Grid.Row="1" Background="#202838" CornerRadius="8" Padding="10">
                        <Canvas x:Name="PreviewCanvas"
                                Background="#111827"
                                SizeChanged="OnPreviewSizeChanged"
                                MouseLeftButtonDown="OnPreviewMouseLeftButtonDown"
                                MouseLeftButtonUp="OnPreviewMouseLeftButtonUp"
                                MouseMove="OnPreviewMouseMove" />
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="22">
                <StackPanel>
                    <TextBlock Text="布局设置" FontSize="16" FontWeight="SemiBold" Foreground="#172033" Margin="0,0,0,18" />
                    <TextBlock Text="选择预设" Foreground="{DynamicResource MutedTextBrush}" FontSize="12" />
                    <ComboBox x:Name="PresetComboBox" Height="34" Margin="0,7,0,16" SelectionChanged="OnPresetSelectionChanged" />

                    <TextBlock Text="区域比例" Foreground="{DynamicResource MutedTextBrush}" FontSize="12" />
                    <StackPanel x:Name="RatioPanel" Margin="0,8,0,16">
                        <TextBlock x:Name="RatioLabel" Text="左侧 80% / 右侧 20%" Foreground="#273248" FontSize="13" />
                        <Slider x:Name="RatioSlider" Minimum="10" Maximum="90" Value="80" TickFrequency="5" IsSnapToTickEnabled="False" Margin="0,6,0,0" ValueChanged="OnRatioChanged" />
                    </StackPanel>

                    <TextBlock Text="实际像素" Foreground="{DynamicResource MutedTextBrush}" FontSize="12" />
                    <TextBlock x:Name="WorkAreaPixelLabel" Text="当前工作区：读取中" Foreground="#273248" FontSize="13" Margin="0,7,0,10" />
                    <StackPanel x:Name="PixelEditorPanel" Margin="0,0,0,8" />
                    <TextBlock x:Name="PixelInputHint" TextWrapping="Wrap" Foreground="{DynamicResource MutedTextBrush}" FontSize="12" LineHeight="18" Margin="0,0,0,8" />
                    <Button x:Name="ApplyPixelButton" Content="应用像素宽度" HorizontalAlignment="Left" Click="OnApplyPixelClick" />

                    <TextBlock x:Name="LayoutDescription" TextWrapping="Wrap" Foreground="{DynamicResource MutedTextBrush}" FontSize="12" LineHeight="19" Margin="0,0,0,20" />
                    <Separator Margin="0,0,0,16" />
                    <CheckBox x:Name="AutostartCheckBox" Content="开机后自动启动并驻留托盘" FontSize="13" IsChecked="True" />
                    <TextBlock Text="普通窗口不会被自动移动；只有执行最大化时才会填充其中心所在区域。" TextWrapping="Wrap" Foreground="{DynamicResource MutedTextBrush}" FontSize="12" LineHeight="18" Margin="0,11,0,0" />
                </StackPanel>
            </Border>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,22,0,0">
            <Button Content="保存为预设" Click="OnSavePresetClick" />
            <Button Content="取消" Click="OnCancelClick" />
            <Button Content="应用" Background="{DynamicResource PrimaryBrush}" Foreground="White" BorderBrush="{DynamicResource PrimaryBrush}" Click="OnApplyClick" />
        </StackPanel>
    </Grid>
</Window>
